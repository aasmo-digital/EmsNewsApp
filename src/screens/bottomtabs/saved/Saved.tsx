import {View, Text} from 'react-native';
import React from 'react';
import {HeaderCompt, PageContainer} from '../../../components/componentsIndex';

const Saved = () => {
  return (
    <PageContainer>
      <View style={{flex: 1}}>
        {/* <HeaderCompt title="Saved News" /> */}
      </View>
    </PageContainer>
  );
};

export default Saved;
